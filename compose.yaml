# Code generated by gen-compose.sh from compose.cue. DO NOT EDIT.
#
# Edit compose.cue and re-run ./gen-compose.sh. That file carries the
# documentation for every setting; cue export cannot emit comments.
services:
  md-bug:
    container_name: md-bug
    entrypoint:
      - /app/md-bug-backend
    environment:
      BUG_ROOT: /app/work
      BUG_PORT: ${MD_BUG_PORT:-7878}
      FRONTEND_DIR: /app/public
      RUST_LOG: ${RUST_LOG:-debug}
      ADMIN_USERNAME: ${MD_BUG_ADMIN_USERNAME:-admin}
      GENERATE_FAKE_DATA: "false"
    image: xerofuzzion/md-bug:${MD_BUG_TAG:-latest-x86_64}
    ports:
      - ${MD_BUG_HOST_PORT:-7878}:${MD_BUG_PORT:-7878}
    restart: unless-stopped
    volumes:
      - ./bug-data:/app/work
volumes:
  md-bug-data: null
